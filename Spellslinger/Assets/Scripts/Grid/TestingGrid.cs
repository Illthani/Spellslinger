using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestingGrid : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GridBase grid = new GridBase(20, 10, 10f);
            
    }

}
